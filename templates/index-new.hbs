{{#*inline "page"}}
{{~#if tags}}
{{~else}}
<header class=tight-headline>
<h2>New posts</h2>
</header>
{{~/if~}}
{{#each tags}}
<header class=tight-headline>
<h2>#{{this.name}}</h2>
<p>{{this.description}}</p>
<nav><a href=.?tag={{this.name}}>Home</a> &bull; <a href=latest?tag={{this.name}}>Latest</a> &bull; <a href=top?tag={{this.name}}>Top</a> &bull; <b>New</b></nav>
</header>
{{/each}}
<ajax-form>
{{#each posts}}
    {{~> view-post~}}
    {{~#if @last~}}
    <p><a href="?after={{this.uuid}}">Next page</a></p>
    {{~/if~}}
{{/each}}
</ajax-form>
{{/inline}}
{{~> (parent)~}}
