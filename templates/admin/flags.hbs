{{#*inline "page"}}
    <div class=admin-list>
        {{#each post_flags}}
        <a class=admin-list-item href="../{{uuid}}"><b>{{title}}</b><br>flagged by {{created_by_username}}</a>
        {{/each}}
    </div>
{{/inline}}
{{~> admin/layout~}}
