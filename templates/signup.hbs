{{#*inline "page"}}
    <h2>Hi there!</h2>
    <form action=consume-invite method=post>
        <input type=hidden name=invite_token value="{{invite_token}}">
        <label>Username <input name=username autofocus></label>
        <label>Password <input name=password type=password></label>
        <button>Log in</button>
    </form>
{{/inline}}
{{~> (parent)~}}
